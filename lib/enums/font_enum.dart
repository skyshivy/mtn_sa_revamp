enum FontName {
  bold,
  extraBold,
  light,
  medium,
  mediumItalic,
}

enum TimeType {
  none,
  fullDay,
  weekDay,
  monthly,
  yearly,
}

enum CallerType {
  allCaller,
  callerGroup,
  specificCaller,
}

enum SelectTimeType {
  fullday,
  time,
  timeDate,
}

enum RepeatYearlyViewAction {
  none,
  monthly,
  yearly,
}

enum ToWhomAction { allCaller, specificCaller, addToSuffle }
